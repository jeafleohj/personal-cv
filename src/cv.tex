%flake.nix Generated from org content, edited to be plain LaTeX (LuaLaTeX).
\documentclass[10pt,a4paper,ragged2e,withhyper]{altacv}

\input{src/preamble.tex}
\begin{document}

\directlua{
  cv = cv or dofile("src/lua/init.lua")
  local lang = token.get_macro("cvlang")
  if lang == "" then
    lang = "es"
  end
  local tagline = (lang == "es") and "Ingeniero de software" or "Software Engineer"
  cv.personalinfo(tagline)
}

\makecvheader

\directlua{
  local lang = token.get_macro("cvlang")
  cv.make_render_cv(lang)
}

\end{document}
